#ifndef GUARD_average_h
#define GUARD_average_h
/*
 From Acellerated C++
 
 average.h
 */

#include <vector>

double median(std::vector<double>);
double mean(const std::vector<double>& vec);
double optimistic_median(const std::vector<double>&);
    
#endif